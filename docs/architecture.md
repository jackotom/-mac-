# 项目结构

这是一个 Electron + React + TypeScript 的 Mac 桌面工具。

- `src/main/`：桌面窗口、日志发现、文件监听、和系统对话框。
- `src/shared/`：卡组解析、炉石日志解析、记牌状态模型。
- `src/renderer/`：应用界面。
- `tests/`：解析和状态模型测试。

## 边界

只读取本机日志文件；竞技场选牌和标准/狂野选牌页需要确认当前套牌时，会在获得 macOS 屏幕录制许可后临时识别炉石窗口文字。不会读取游戏内存、注入炉石进程或做自动操作，画面也不会上传或保存。

## 当前能力

- 自动扫描常见 Mac 炉石日志路径。
- 手动选择日志文件或日志目录。
- 修复炉石 `log.config`，打开 Power/Zone 等日志输出。
- 导入炉石卡组代码、炉石导出文本或手写卡组列表。
- 自动同步炉石“我的收藏 / 套牌”里的本机套牌，并写入软件数据库。
- 对局开始后优先读取 `Decks.log` 的已选套牌记录；无法直接确认时再根据真实抽牌/出牌日志自动匹配收藏套牌，不要求用户手动设置当前牌库。
- 收藏记录的基础卡牌数量必须与当前 `Power.log` 的本方牌库快照一致；开局额外生成的牌会单独计入实时总数，不会阻止已选套牌识别。不一致时不允许旧套牌覆盖实战数据，而是显示真实牌库数量并等待精确匹配。
- 监听中的炉石重开会创建新的日志会话目录；如果最新会话暂时没有 `Power.log` 且没有可用竞技场牌库，服务会提示修复日志、清空旧状态并继续检查该会话，`Power.log` 出现后自动恢复，不能回退显示旧会话牌库。
- 竞技场选牌时读取 `Arena.log` 和 `Power.log`；当前版本不输出候选牌的国服客户端会自动识别炉石窗口内的三张牌名，随后显示真实评分和胜率。
- 标准/狂野选牌页会识别画面里的模式标题和右侧当前套牌名，用于在对局开始前把悬浮窗切到正确收藏套牌；同名套牌必须用模式区分。
- 冷启动时即使新会话暂时只有 `Decks.log`、还没有 `Power.log`，也会启动标准/狂野选牌页识别，不需要先进入竞技场来激活记牌小窗。
- 确认标准/狂野套牌后自动打开置顶小窗；开局时保留刚确认的套牌，对局暂时无法精确识别时也保持“正在识别”小窗。进入竞技场时复用同一主小窗并切换内容。炉石不在前台时自动隐藏，返回炉石后恢复，不会抢走游戏焦点。
- 标准/狂野模式同时驱动左侧天梯推荐窗。主进程严格校验国服、版本、胜率、场次和卡组代码，只从当前版本且达到最低场次的记录中选择最高胜率卡组；没有可信来源时明确显示不可用。
- 普通启动只在后台监听；点击 Dock 图标或再次打开应用时显示主窗口。主记牌小窗会保存上次位置和大小，并在显示器变化后自动移回可见区域。
- 竞技场评分按版本缓存，只在首次使用或评分版本变化时更新；完成选牌后即使还没生成 `Power.log`，也会从 `Arena.log` 自动生成竞技场牌库，并接入普通记牌面板。
- 从暴雪官方卡牌浏览器导入全量传统对战卡牌并缓存到本机数据库；保留 HearthstoneJSON 的卡牌编号映射用于日志匹配。
- 主程序可从本机缓存分页浏览可收藏卡牌，按关键词、职业和类型筛选，并显示卡图与完整效果。
- 牌库、对手出牌和竞技场候选牌接入卡牌缩略图、法力值、攻击/生命、战吼/技能文本及关联牌详情。
- 支持手动打开或关闭独立置顶小窗；手动关闭后保持到套牌或模式真正变化。
- 主窗口可最小化到 Dock；对手出牌可单独打开为独立置顶小窗。
- 根据日志里的区域变化统计我方抽牌、我方出牌、对手出牌。

## 发布物

- 唯一保留的安装包路径是 `outputs/炉石记牌器.app`。
- 唯一保留的压缩包路径是 `outputs/炉石记牌器-mac-arm64.zip`。
- `npm run package:mac-arm64` 每次都会覆盖这两个固定路径，并清理旧的 `release-*`、历史应用副本和历史版本压缩包。
- macOS 包使用固定标识 `cc.acyg.hearthstonemactracker`，避免系统录屏权限因为包身份变化而反复要求授权。
- 打包脚本必须使用本机有效的 `Apple Development` 证书签名；不再使用临时签名。签名后必须执行 `codesign --verify --deep --strict`，确保录屏授权绑定到稳定应用身份。
- 安装包 `Info.plist` 必须包含 `NSScreenCaptureUsageDescription`，说明画面只用于本地识别模式、套牌和竞技场候选牌；缺少该声明的包不得发布。

## 后续版本

- 用真实对局日志继续补全更多事件格式。
- 对局历史和胜率统计。

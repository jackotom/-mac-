# Task 9 验收报告

## 结论

卡牌生命周期回放、详情展示和真实 Electron 窗口验收已完成。

- 当前设备可执行的 7 个真实窗口场景全部通过。
- `friendly-tall` 未伪造通过：当前所有显示器的真实 `workArea` 只有
  `[{"x":0,"y":33,"width":1470,"height":834}]`，不满足严格要求的
  `100×900`。脚本会明确失败并输出实际显示器数据。
- 未执行 Task 10，未改版本号，未打包。

## 完成内容

- 新增 3 份脱敏固定日志：
  - 满手爆牌与非满手反例
  - 卡牌打出、回手、再次打出、延迟揭示
  - 尤格 5 次与 10 次施法、重复法术、嵌套尤格
- 新增生命周期回放测试，验证事件去重、实体复用、延迟揭示、根节点顺序、
  重复法术保留和嵌套结果。
- 新增 8 场景真实 Electron 验收脚本：
  `friendly-short`、`friendly-tall`、`opponent-secret`、
  `opponent-unknown-hand`、`inline-normal`、`inline-pinned`、
  `external-normal`、`external-pinned`。
- QA 检查会读取所有 Electron 显示器，核对窗口和视口尺寸、完整滚动归属、
  未公开手牌、详情展开、固定/取消固定、5/10 次结果、重复与嵌套结果，
  并统计渲染错误。
- 使用真实 `KeyboardEvent` 和正式的详情固定逻辑，不用测试专用替代实现。

## 真实验收发现并修复

真实小窗口检查发现生命周期外层容器抢走滚动，已限制容器高度，卡牌主区域成为
唯一滚动区域。

真实详情窗口检查发现结果树和卡池各自生成内部滚动条，已移除内部滚动，详情只由
外壳统一滚动。两处均补了静态与界面回归测试。

## 安全与隔离

- 每个场景使用独立临时 `userData`。
- 只把仓库内固定日志复制为临时 `Power.log`，只读取临时卡牌缓存和固定牌组。
- 子进程显式设置临时日志目录，并清空可能从父环境继承的其他 QA 启动开关。
- 真实验收曾发现父环境的 `QA_START_TRACKING` 会让捕获流程切回系统游戏日志；
  已修复，并通过状态检查确认最终只读取临时日志。
- 清理只针对本任务创建的进程和临时目录。

## 验证结果

- 回放与界面专项测试：18/18 通过。
- 全量测试：116 个测试文件通过；861 个测试通过，1 个按原计划跳过。
- 类型检查：通过。
- 构建：通过。
- 差异格式检查：通过。
- 真实 Electron：
  - `friendly-short`：通过
  - `opponent-secret`：通过
  - `opponent-unknown-hand`：通过
  - `inline-normal`：通过
  - `inline-pinned`：通过
  - `external-normal`：通过
  - `external-pinned`：通过
  - `friendly-tall`：因真实显示器高度 834，不满足 900，按要求明确失败
- 首次真实运行缺少本地辅助程序，已执行项目自带构建命令生成后重新验证。
- TokenSave 在代码定位阶段节省约 40,000 tokens。

## 剩余限制

只剩真实高屏场景需要在 `workArea.height >= 900` 的显示器上执行：

```bash
npm run verify:card-lifecycle-ui
```

脚本不会在不满足条件时宣称 8 个场景全部通过。

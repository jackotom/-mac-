# Task 7 前端段验收报告

## 结果

- 卡牌详情只保留 `summary`、`interactive` 两种模式，所有 renderer 调用点显式选择模式。
- 普通悬浮不展示理论候选池；实际施放和触发结果完整展示。
- 可操作详情的理论池默认折叠，首批 12 张，每次继续显示 12 张；实际结果不分页。
- 内嵌悬浮支持真实 `Alt+Q` 固定、再次 `Alt+Q` 或 `Escape` 取消；固定后为可操作对话框，目标卸载会清理。
- 外部卡牌窗只根据主进程 pinned IPC 切换详情模式，renderer 键盘事件不会伪造外部固定状态。
- 主窗口对手数据改读 `cardTracking`：手牌、牌库、奥秘、已使用记录都不再读取旧字段；疲劳读取公开计数；未知回合显示 `?`。
- 主窗口事件流不再按数组顺序伪造回合；没有真实回合字段时全部显示 `?`。
- 顶部状态把事件数量明确标为“事件 N”，不再把事件数冒充日志解析行数。
- Task 6 改版后的 QA 测试已同步：当前/历史分页按真实点击验证，候选隐藏不再误判为奥秘槽消失，窗口最小高度断言改为 200。

## TDD

- RED：新增关键测试后，7 项按预期失败，覆盖理论池误展示、错误的 60 张分页、内嵌无法固定、外部模式不跟随 IPC、首页继续读取旧字段。
- 复审 RED：新增 2 项测试后按预期失败，分别抓到三条事件被伪造成回合 `1/1/2`，以及顶部错误显示“3 行”。
- GREEN：Task 7 renderer 聚焦测试 11 个文件、85 项全部通过。

## 自动验证

- 全量测试：114 个文件通过；892 项通过，1 项跳过，0 失败。
- 类型检查：通过。
- 生产构建：通过。
- `git diff --check`：通过。

## EgoLite 实测

- 外部 summary：理论池不渲染；25 条实际结果全部存在。
- 外部 pinned IPC：理论池出现且默认折叠；首批 12 张；继续后 24 张；固定提示正确；页面横向溢出为 0。
- 内嵌 hover：普通状态为 tooltip；真实 `Alt+Q` 后变为 dialog，允许点击和滚动；理论池默认折叠且首批 12 张；`Escape` 后 dialog 清除。

## 自审

- 未修改 `src/main`、主进程测试和对手奥秘 presenter。
- 未进入 Task 8。
- 未发现残留的 renderer `CardDetailBody` 隐式模式调用。
- TokenSave 原任务代码定位减少约 85,000 tokens；本次复审额外减少约 26,700 tokens。
